name: Test pull_request_comment event
on:
  pull_request_review_comment:
    types: [created, edited]
  issue_comment:

jobs:
  test_features:
    runs-on: ubuntu-latest
    steps:
      - run: echo "I was edited"

