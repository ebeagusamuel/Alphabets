name: Test pull_request_comment event
on:
  pull_request_review_comment:
    types: [created, edited]

jobs:
  test_features:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/action/anvil-reminders

